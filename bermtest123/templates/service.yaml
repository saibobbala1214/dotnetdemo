apiVersion: v1
kind: Service
metadata:
  name: {{ include "bermtest123.fullname" . }}
  labels:
    {{- include "bermtest123.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bermtest123.selectorLabels" . | nindent 4 }}
