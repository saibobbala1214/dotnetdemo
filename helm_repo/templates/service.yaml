apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_repo.fullname" . }}
  labels:
    {{- include "helm_repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_repo.selectorLabels" . | nindent 4 }}
